// ArrayList is slower for inserts and deletes at the beginning,
// because it must shift many elements in memory.

// LinkedList is slower for random access,
// because it must walk through nodes one by one.